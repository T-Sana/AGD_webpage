<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/public/favicon.ico" />
    <link rel="stylesheet" type="text/css" href="/style.css" />
    <title>AGD | <%= DBN %> Ajout ISBN</title>
</head>
<body>
    Ceci est la page d'ajout par ISBN à la DB <i><%= DBN %></i>.<br><br>
    <form method="post", action="/src/mod/db/<%= DBN %>/add">
        <% Object.keys(_livre_).forEach((key) => { %>
            <% console.log(infos) %>
            <%= key %>: <input type="text" value="<%= infos[key] %>" name="<%= key %>"><br>
        <% }) %>
        <input type="submit" value="Confirmer">
    </form><br>
    Retourner à l'<a href="/">acceuil</a><br>
    Retourner à l'<a href="/src/db/<%= DBN %>">acceuil de la DB</a>
</body>
</html>